# Put these three files in the same folder:
#   app.py
#   mavuno_foods_bess_challenge.xlsx   ← your original file
#   requirements.txt

pip install -r requirements.txt
streamlit run app.py
python -m streamlit run app.py